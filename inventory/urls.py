from django.urls import path
from . import views     # Importer les vues que je crééerais

urlpatterns = [
    # path('', views.index, name='index'),      # Route pour la page d'acceuil de l'application
    # path('add/', views.add_item, name='add_item'),      # Route pour ajouter un élément
]